# 差异分析 — 插件 vs 现有 northstar-tools/CLAUDE.md

> 对比基准：现有 `/home/cy/northstar-tools/CLAUDE.md`
> 对比对象：`CLAUDE_CONSTRAINT_PLUGIN.md` + `template/CLAUDE_CONSTRAINT_CHUNK.md`

---

## 1. 已有内容（现有 CLAUDE.md 已覆盖，不宜重复）

以下内容**已在现有 northstar-tools/CLAUDE.md 中存在**，嵌入约束区块时应注意去重或替换：

| 已有项 | 现有位置 | 差异程度 |
|--------|----------|----------|
| 身份定义（BLD/铁甲虾） | 开头「身份」区块 | 基本一致，插件更强调＂Hermes虾系编制＂ |
| 工作目录 | 「工作目录」区块 | 插件使用模板变量 {{}} |
| 职责边界（做什么/不做什么） | 「职责边界」区块 | 现有8条红线，插件扩展至12条 |
| 输出规范（README/ACCEPTANCE/CHANGELOG） | 「输出规范」区块 | 完全一致 |
| B3五项一致性验收 | 「验收标准（B3规范）」区块 | 完全一致 |
| 当前工具链 | 「当前工具链」表格 | 模板占位符，需要项目填充 |
| 当前观察期纪律 | 「当前观察期纪律」区块 | 模板占位符，需要项目填充 |
| 小补丁施工模式 | 「小补丁施工模式及双入口执行策略」区块 | 插件不含此内容（属于CC执行策略，非约束） |

## 2. 新增内容（现有 CLAUDE.md 缺失，核心贡献）

以下内容**在现有 northstar-tools/CLAUDE.md 中缺失**，是此插件最大的增量价值：

### 2.1 八大铁律（完全新增）

| 铁律 | 现有CLAUDE.md | 插件 |
|------|--------------|------|
| ① 不越权 | 隐含在红线列表中 | ✅ 独立成§2.1，有违规后果 |
| ② 不死循环 | ❌ 缺失 | ✅ 有停止条件要求 |
| ③ 思维透明化 | ❌ 缺失 | ✅ 有详解和强制规则 |
| ④ 必须留痕 | ❌ 缺失（仅限＂不直接写task_ledger＂） | ✅ 有完整定义 |
| ⑤ 必须可验收 | 隐含在B3验收中 | ✅ 有明确定义 |
| ⑥ 代码极简化 | ❌ 缺失 | ✅ 有详解 |
| ⑦ 修改精准化 | ❌ 缺失 | ✅ 有详解 |
| ⑧ 失败必须收敛 | ❌ 缺失 | ✅ 有降级链路 |

### 2.2 八条执行纪律（完全新增）

| # | 纪律 | 现有CLAUDE.md | 插件 |
|---|------|--------------|------|
| ①-⑧ | 全部8条 | ❌ 缺失 | ✅ 完整 |

### 2.3 任务自动升级规则（C1 / A2 / A1 衍生）

- 文件生成→自动升级V2 → 新增
- V3强审计5项要求 → 新增
- 自动分诊收尾流程 → 新增

### 2.4 失败处理规则

- 第1次/第2次/MAIN失败三级链路 → 新增
- 模型失败处理 → 新增
- 基础设施变更上报 → 新增

### 2.5 BLD施工纪律（从skill提取，现有CLAUDE.md未完整包含）

| 纪律项 | 现有CLAUDE.md | 插件 |
|--------|--------------|------|
| 任务目录隔离 | ❌ 缺失（仅在bld-low-cost-workflow skill中） | ✅ |
| 安全工具类fake fixtures | ❌ 缺失 | ✅ |
| 仅Python stdlib | ❌ 缺失 | ✅ |
| 只读扫描约束 | ❌ 缺失 | ✅ |
| MAIN不陪跑 | ❌ 缺失 | ✅ |
| 禁止长链fallback | ❌ 缺失 | ✅ |

### 2.6 自检清单

| 自检项 | 现有CLAUDE.md | 插件 |
|--------|--------------|------|
| 6项BLD自检清单（铁律③⑥⑥⑦④⑤） | ❌ 缺失 | ✅ §6 |
| 禁止操作清单（12条硬红线） | 仅8条 | ✅ 扩展至12条 |

### 2.7 来源可追溯

| 特性 | 现有CLAUDE.md | 插件 |
|------|--------------|------|
| 每条约束的出处文件 | ❌ 缺失 | ✅ SOURCE_INDEX.md |

## 3. 去重与合并策略

### 推荐方案A：替换式（现有 northstar-tools/CLAUDE.md）

1. 保留现有CLAUDE.md的：身份/工作目录/工具链/当前阶段/小补丁模式
2. 替换现有CLAUDE.md的约束区块为：`template/CLAUDE_CONSTRAINT_CHUNK.md`
3. 现有职责边界红线8条 → 与插件12条合并（保留差异）

### 推荐方案B：追加式（其他项目CLAUDE.md）

1. 复制 `template/CLAUDE.md_template.md` 为起点
2. 按需填入项目特有内容（工具链/目录/阶段）
3. 约束区块直接使用，不需修改

### 推荐方案C：最小嵌入（快速约束）

1. 在目标 CLAUDE.md 尾部追加 `template/CLAUDE_CONSTRAINT_CHUNK.md` 的内容
2. 删除原 CLAUDE.md 中与 §1 已有内容重复的部分

## 4. 无冲突声明

- 不覆盖现有 northstar-tools/CLAUDE.md（插件输出到独立目录）
- 不修改 gateway/config.yaml/.env（插件为纯文本约束层）
- 不接入运行时环境（无 cron/tmux/systemd/webhook）
- 不检查运行时状态（无 PostgreSQL/Qdrant/API 调用）
